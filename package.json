{
  "name": "easywebui-js",
  "version": "2.0.0",
  "description": "Lightweight CSS Framework with jQuery plugins",
  "main": "EasyWebUI.js",
  "engines": {
    "node": "^5.0.0"
  },
  "devDependencies": {
    "requirejs": "^2.3.3",
    "uglify-js": "^2.8.28"
  },
  "scripts": {
    "bundle": "r_js -o build/source.js",
    "compress": "uglifyjs EasyWebUI.js -c -m -o EasyWebUI.min.js --source-map=EasyWebUI.min.map",
    "build": "npm run bundle  &&  npm run compress",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://git.oschina.net/Tech_Query/EasyWebUI.git"
  },
  "keywords": [
    "css",
    "framework",
    "jquery",
    "lightweight"
  ],
  "author": "shiy007@qq.com",
  "license": "GPL-2.0"
}
